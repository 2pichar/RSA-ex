{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: start:ts",
      "request": "launch",
      "command": "npm run start:ts",
      "cwd": "${workspaceFolder}"
    }
  ]
}